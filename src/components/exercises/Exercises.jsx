import React from 'react'
import { useSelector } from 'react-redux'

export const Exercises = ({nombre}) => {
   
    
    return (
        <div>
            {nombre}
        </div>
        /*Aqui para crear la tabla añadiremos todo lo que es el registro, con usuario, fecha e id de ejercicio */
    )
}
